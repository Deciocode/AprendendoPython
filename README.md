# 📊 Primeiros Passos em Python: Cálculos Financeiros e Tipos de Dados

Este repositório contém um conjunto de scripts Python básicos desenvolvidos como parte do meu processo inicial de aprendizado da linguagem. O objetivo principal é praticar 
a declaração de variáveis, operações matemáticas, e a manipulação de estruturas de dados fundamentais.


---

## 🎯 Conceitos Abordados

O projeto demonstra o uso dos seguintes pilares do Python:

### 1. Tipos de Dados Primitivos
* **Inteiros (`int`)**
* **Ponto Flutuante (`float`)**
* **Strings (`str`)**
* **Booleanos (`bool`)**

### 2. Operações Aritméticas e Lógicas
* Cálculo de **Lucro** e **Margem de Lucro**.
* Uso do operador **Módulo (`%`)** para obter o resto da divisão.
* Conversão de tipos (`int()`, `float()`).

### 3. Interação e Estruturas de Dados
* Uso da função **`input()`** para obter dados do usuário.
* Manipulação e limpeza básica de dados de entrada (usando `.replace()`).
* Introdução à estrutura de dados **Lista (`list`)** e funções de agregação como **`sum()`** e **`len()`**.

---
